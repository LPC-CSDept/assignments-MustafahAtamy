#include<iostream>
#include<fstream>//for file operations
#include<iomanip>

using namespace std;
const unsigned short N=5;
struct scores {
  double sc[N];
  string grade;
  };
 struct grade {
  string first;
  string last;
  string ssn;
  scores score;
  };
  
  Grade g; 
  void printstruct(Grade);
  int main()
  {
  fstream fs;
  string line;
  ifstream ini_file {"grades.txt"};
  ofstream out_file {"grades.bin"};
// Declare fstream for ofstream and ifstream class object. Declare string line for writing text file.
if(ini_file && out_file) {
while(getline(ini_file,line)) {
out_file <<line<< "/n";
}
cout<<"Cannot read file."<<endl;
